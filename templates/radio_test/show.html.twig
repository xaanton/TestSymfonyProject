{% extends 'base.html.twig' %}

{% block title %}RadioTest{% endblock %}

{% block body %}
    <h1>RadioTest</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ radio_test.id }}</td>
            </tr>
            <tr>
                <th>SelectedValue</th>
                <td>{{ radio_test.selectedValue }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('radio_test_index') }}">back to list</a>

    <a href="{{ path('radio_test_edit', {'id': radio_test.id}) }}">edit</a>

    {{ include('radio_test/_delete_form.html.twig') }}
{% endblock %}
